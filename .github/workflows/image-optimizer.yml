name: 🖼️ Shopify Image Optimizer

on:
  schedule:
    - cron: '0 14 * * *'
  workflow_dispatch:

jobs:
  optimize:
    name: 🧠 Optimize Alt Texts
    runs-on: ubuntu-latest
    env:
      SHOPIFY_STORE: ${{ secrets.SHOPIFY_STORE }}
      SHOPIFY_TOKEN: ${{ secrets.SHOPIFY_TOKEN }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '18'
      - run: npm ci
      - run: node alt-image-optimizer.js
      - name: ✅ Slack success
        if: success()
        run: |
          curl -X POST $SLACK_WEBHOOK_URL \
          -H 'Content-type: application/json' \
          --data '{"text":"✅ Alt texts optimisés avec succès à 16h00."}'
      - name: ❌ Slack fail
        if: failure()
        run: |
          curl -X POST $SLACK_WEBHOOK_URL \
          -H 'Content-type: application/json' \
          --data '{"text":"❌ Échec optimisation Alt images à 16h00. À vérifier."}'
