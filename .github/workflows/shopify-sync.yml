name: 🛍️ Shopify Products Sync

on:
  schedule:
    - cron: '0 14 * * *' # 16h Paris
  workflow_dispatch:

jobs:
  sync:
    name: ✨ Sync Products
    runs-on: ubuntu-latest
    env:
      SHOPIFY_STORE: ${{ secrets.SHOPIFY_STORE }}
      SHOPIFY_TOKEN: ${{ secrets.SHOPIFY_TOKEN }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '20'
      - run: npm ci
      - run: node index.js
      - name: ✅ Slack success
        if: success()
        run: |
          curl -X POST $SLACK_WEBHOOK_URL \
          -H 'Content-type: application/json' \
          --data '{"text":"✅ Produits Shopify mis à jour avec succès à 16h00."}'
      - name: ❌ Slack fail
        if: failure()
        run: |
          curl -X POST $SLACK_WEBHOOK_URL \
          -H 'Content-type: application/json' \
          --data '{"text":"❌ Échec sync produits à 16h00. Vérifie GitHub Actions."}'
